import { ProjectExplorer } from "@/components/ProjectExplorer";

export default function NewProjectsPage() {
  return (
    <ProjectExplorer
      title="Nuevos proyectos"
      subtitle="New"
      tagFilter="Nuevo"
      infoText="Descubre campañas recién publicadas y súmate desde el inicio."
    />
  );
}
